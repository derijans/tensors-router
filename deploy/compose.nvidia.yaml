x-tensors-router-deprecated: "Use compose.cuda.yaml or compose.vllm-cuda.yaml for images with the CUDA user-space runtime."

services:
  router-node:
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: ["gpu"]

  router-webui:
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: ["gpu"]
